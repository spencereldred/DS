	<%= form_for @route, url: route_preview_path do |f| %>
		<%= f.label :starting_point%>
	  <%= f.text_field :starting_point, :id => "autofill" %>	
	  <br>
	  <%= f.label "Destination:" %>
	  <%= f.text_field :end_point, :autofocus => true, :id => "autofill" %>
	  <br>

	  <p><p>
	  <%= f.submit  class: "btn btn-primary" %>
	<% end %>
